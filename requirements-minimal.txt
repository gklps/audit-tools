# Minimal requirements for Python 3.8+ compatibility
# Use this if the main requirements.txt fails

# Core ODBC driver for Azure SQL Database
pyodbc>=4.0.0

# HTTP requests for IP detection and IPFS
requests>=2.25.0

# Data processing (use older pandas for Python 3.8)
pandas>=1.3.0,<2.1.0

# Optional: SQLAlchemy for advanced database operations
# sqlalchemy>=1.4.0,<2.0.0

# Telegram notifications (minimal version)
python-telegram-bot>=13.0,<21.0